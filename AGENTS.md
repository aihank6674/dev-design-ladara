# Agent Instructions

## Agent skills

### Issue tracker

Issues are tracked using GitHub Issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Standard triage label vocabulary is used (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at repo root). See `docs/agents/domain.md`.
